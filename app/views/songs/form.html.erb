<h1><%= @song.id ? "Edit Song" : "Create Song" %></h1>
<hr/>

<%= form_for @song do |f| %> 
    <%= f.label :name %>
    <%= f.text_field :name, required: true, autofocus: true %>
    <br />
    <%= f.submit %>  
<% end %> 